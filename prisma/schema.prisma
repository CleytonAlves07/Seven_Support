generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

model Tecnico {
  id   String @id @default(uuid())
  nome String

  @@map("tecnicos")
}

// Incluir tabela clientes
